# Core pipeline (corpus parsing, normalization, KG, BM25 retrieval, discovery,
# hypothesis cards, CLI) runs on the Python standard library alone — no install
# needed beyond Python 3.9+.

# Optional: HTTP API layer (herb_hermes.api.server)
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27        # only for the FastAPI TestClient / smoke tests

# Optional: LLM / agent layer (herb_hermes.llm) — lazy-imported
litellm>=1.40      # unified client for OpenAI / Anthropic / local models

# Optional: MCP server for Claude Code / Codex (herb_hermes.mcp_server)
# mcp[cli]>=1.2

# Optional: voice backends are heavy + GPU-bound; install per the notebook
# (FireRedASR2S, CosyVoice3, torch, torchaudio) — NOT pulled in by default.

# Optional: test runner
pytest>=8.0
